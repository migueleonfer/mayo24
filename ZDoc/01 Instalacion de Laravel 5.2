
Variables de Ambiente:
=====================



1.1) Instalar Laravel

	--	composer global require "laravel/installer"
	
1.2) Creacion del Proyecto

	--	composer create-project --prefer-dist laravel/laravel mayo24
	
	--  compruebas con:   localhost/traste/public
	
1.3) ENV y COndifg/databse.php
	--	Puerto 5432
	--	DB, User y Passw.


	
2) Tablas  :   	Para correr o iniciar nuestras migraciones usamos el comando:  php artisan migrate
				Con esto si es la primera vez que se ejecuta este comando se creará en nuestra base de datos la tabla migrations
	2.1.	crear_tabla_pasteles y usamos el comando:


	        php artisan migrate  <========================!!

			- php artisan migrate:rollback que lo que hará es deshacer la última migración ejecutada y registrada en la base de datos.
			- php artisan migrate:reset que lo que hará es deshacer todas las migraciones de la base de datos.
			- php artisan migrate:refresh, el cual es equivalente a usar:
						  - php artisan migrate:reset y después 
						  - php artisan migrate.

			- Se puede agragar o bajar columnas

	2.2.	SEEDERS, se crea el proceso de relleno de datos de prueba, ficticios y se registran en 
		             php artisan db:seed
		    NOTA: en cado de ERROR: composer dump-autoload           <========================!!

			- php artisan make:seeder nombre_seeder
			- php artisan make:seeder GeograficoSeeder   Crea la estructura para ser rellenada con el Faker.
			- Instalar Facker
			- Rellenar las columnas con valores Faker
			- Rellena de datos todas las tablas que estan ENVphp artisan db:seed 
			  - php artisan db:seed
			- Rellena una sola tabla
			  -			

			- Por defecto, el comando db:seed ejecuta la clase DatabaseSeeder, la cual puede ser utilizada para llamar a otras clase pobladoras. Sin embargo, se puede especificar la opción --class para ejecutar una clase pobladora específica de forma individual:			  	

			 Unos comandos extras que nos pueden ser utiles son:

			-	php artisan migrate --seed

			php artisan db:seed 		==> Rellena  todas  las tablas

			php artisan db:seed --class=GeograficoSeeder   => Rellena  una sola tabla


			El comando anterior lo que hace es realizar una combinación entre los comandos php artisan migrate y php artisan db:seed.

			-	php artisan migrate:refresh --seed

			El comando anterior lo que hace es realizar una combinación entre los comandos 
			php artisa¿'' migrate:refresh     y    php artisan db:seed.





